#!/bin/sh

# File env vars / application requirements
export COG_FILESIZE_LIMIT=51200 # In Bytes (51.200 B == 50Kb)
export COG_FILENAME_PREFIX="EduardoChallenge"

# Server env vars / server setup info
export COG_SERVER_PORT=1024
export COG_SERVER_TIMEOUT=5000 # Milliseconds
